# 南亞技術學院 - 自動排課系統 專案記錄 (gemini.md)

## 1. 專案現況與目標
建立一個基於 Google OR-Tools (CP-SAT Solver) 的高效率自動排課系統，旨在解決大學跨年級、合班、多重約束下的排課難題。

## 2. 核心技術架構
- **後端**: Python (FastAPI) + SQLAlchemy
- **資料庫**: PostgreSQL (Docker 容器化)
- **求解器**: Google OR-Tools (基於約束編程 CP-SAT)
- **前端**: 原生 HTML5 + CSS3 + JavaScript (支援分頁管理與動態規則調整)

## 3. 已實作排課規則 (核心邏輯)

### A. 基礎硬限制 (絕對遵守)
1. **教師/教室/班級不衝堂**: 確保資源在同一時段不重複佔用。
2. **連續上課**: 2-3 學分的課程視為連續區塊，禁止拆散排課。
3. **午休隔斷 (4/5節禁跨)**: 課程區塊嚴禁橫跨第 4 節（上午最後一節）與第 5 節（下午第一節）。
4. **合班課同步**: 支援一門課程多選班級，確保參與的所有班級在同一時段、教室上課。

### B. 時間禁區限制
1. **週五禁排**: 除「國際產專班」外，其餘班級週五全天禁排。
2. **週四下午禁排**: 第 5 節起全校統一不排課。
3. **教師禁排限制**: 藍中賢老師（副校長）與林金俊老師（主任），週二下午（第 5-8 節）開會禁排。
4. **通識預留區**: 週一第 5-8 節為通識專用。**非通識課程絕對禁入**。

### C. 節次開放規則
1. **週一全開放**: 開放第 1 節至第 8 節排課。
2. **週二至週四限縮**: 預設僅開放第 2-7 節（可透過設定開關）。
3. **勞作教育特權**: 名稱含「勞作」且設定了「課程特許節次」者，無視全校性節次限縮，可排在指定的 1 或 8 節。

### D. 非強制性希望規則 (軟限制)
1. **希望起始時段**: 使用者可為個別課程設定「希望起始天/節」。求解器會賦予 1000 分加分，在不衝突前提下優先達成。

## 4. 資料模型設計 (Database Schema)
- **Teacher**: 包含 `is_director` 標記。
- **Classroom**: 區分一般、電腦、體育、專題等類型。
- **ClassGroup**: 支援不分年級，以班級名稱為主。
- **Course**: 支援多對多班級關聯 (`classes`)，具備 `allowed_slots` (特許節次) 與 `fixed_slot` (希望起始)。
- **SystemSetting**: 儲存所有可動態調整的禁排天數、節次集合。

## 5. 115-1 學期資料整合 (2026-05-10)
- **環境設定**: 已在虛擬環境 (`.venv`) 安裝 `pandas` 與 `openpyxl` 以處理 Excel 資料。
- **資料提取**: 透過 `generalist` 子代理程式與自定義腳本，成功從 `J-自動排課系統` 的真實 Excel 檔案中提取老師與課程資料。
- **自動匯入**: 實作 `backend/init_115_1_data.py`，可一鍵將 115-1 的真實課表需求（包含資工系各年級、國際產專班、共同科目）匯入資料庫。
- **教室識別**: 根據課程備註自動識別「電腦教室」、「無人機實作場」等特殊資源需求。

## 6. 115-1 排課優化與效能提升 (2026-05-10)
- **教室約束瓶頸解決**: 發現未指定教室之課程過度擠壓 2 間一般教室導致 UNKNOWN。透過擴大候選教室至「一般+電腦」組合，成功將求解時間降至數秒內。
- **啟發式策略導入**: 加入 `AddDecisionStrategy` 優先決定時間段，並配合 12 核心並行搜索。
- **自動驗證機制**: 建立 `backend/verify_solution.py` 進行產出結果的二次校核，確保 0 衝堂。
- **規則微調**: 
    - 每節課上限 9 節。
    - 班級負載 > 32 小時者自動開放週五。
    - 修正午休隔斷判定邏輯。

## 7. 待辦/後續優化
- [ ] 增加課程分佈均勻度權重（避免某班集中在某一天）。
- [ ] 教師連上課時數上限設定。
- [ ] 評估合班課之教室容量檢查。

## 8. 115-1 課程規劃表匯入與功能更新 (2026-05-12)
- **資料來源更新**: 實作 `backend/init_from_summary.py`，直接從 `course_summary.md` 提取 115-1 教師授課規劃表。
- **保留課程支援**: 
    - `Course` 模型新增 `is_reserved` 與 `should_schedule` 欄位。
    - 標記為「(保留課程)」的項目預設 `should_schedule=False`。
- **前端優化**: 
    - 在課程管理頁籤增加「參與排課」勾選框與「保留課」警示標籤。
    - 支援使用者手動篩選欲排入求解器的課程。
- **排課引擎優化**: 
    - 求解器自動過濾未啟動排課的課程。
    - 主任禁排規則改為動態讀取 `SystemSetting` 中的節次設定。
    - 自動過濾學分 > 4 或名稱含「實習」的非實體課，確保求解可行性。
    - **[2026-05-12 新增]**: 拔除了基於高負載自動放寬的特權機制，強制遵守「週四下午禁排」與「週五禁排」。
    - **[2026-05-12 新增]**: 實作「一上沒通識」邏輯，為名稱含「四技一」的班級解鎖週一下午第 5-8 節的通識禁排區，釋放可用格子。
    - **[2026-05-12 新增]**: 實作「上午連三堂強制從第二節開始」的規則，當 3 學分課程安排在上午 (第 1-4 節) 時，強制將起始節次固定為第 2 節。

## 9. 生產環境部署驗證 (2026-05-12)
- 成功透過 `docker-compose` 啟動 PostgreSQL 容器並初始化 `course_schedule` 資料庫。
- 測試 115-1 真實資料在 Docker 環境下執行排課引擎，即便「日四技一」學分較高，在通識禁區解鎖與無效課程過濾後，依然順利無報錯產出完美課表（未打破禁排規則）。
## 10. 504 Gateway Timeout 修復與異步優化 (2026-05-19)
- **問題診斷**: 發現排課引擎預設使用 16 個搜尋執行緒，在資源有限的伺服器上會導致 CPU 飽和，進而觸發 Nginx 或網關的 504 Timeout。
- **後端優化**: 
    - 將 `num_search_workers` 調降至 4，平衡效能與穩定性。
    - 完善 `BackgroundTasks` 機制，新增 `is_scheduling` 狀態旗標防止重複啟動。
    - 調整 `/api/schedule` 回傳格式，支援狀態追蹤。
- **前端優化**: 
    - 實作 **Polling (輪詢) 機制**，每 5 秒檢查一次排課進度。
    - 修正 POST 請求後不等待長連接，立即回傳啟動訊息，徹底解決逾時問題。
    - 增加更友善的載入中狀態顯示（按鈕自動切換為「運算中」）。
- **部署同步**: 已將上述修正同步至 `deploy_package` 目錄。

---
**最後更新日期**: 2026年5月19日
**當前狀態**: 504 Timeout 問題已修復，異步排課機制完善。
---
